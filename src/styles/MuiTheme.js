import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    app_ballet: "#606c38",
    app_salad: "#52b69a",
    app_blue: "#0466c8",
    app_red: "#ef233c",
  },
});

export default theme;
